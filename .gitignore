*.iml
.gradle/
**/build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
*.keystore
!debug.keystore
# Committed on purpose: a stable signature is what lets the app update itself in place. Throwaway
# testing key with a published password — the Play signing key lives in repo secrets, never here.
!app/zephyr-dev.keystore
keystore.properties

# Kotlin compiler daemon working directory
.kotlin/

# Node (the prototype dev server has no dependencies, but just in case)
node_modules/
